# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUI-MVVM-Combine' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUI-MVVM-Combine

  target 'SwiftUI-MVVM-CombineTests' do
    inherit! :search_paths
    # Pods for testing

  pod 'Alamofire', '~> 5.2'

  end

  target 'SwiftUI-MVVM-CombineUITests' do
    # Pods for testing
  end

end
